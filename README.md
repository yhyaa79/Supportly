# AI Web Chat Support – Persian Live Assistant Widget

یک ویجت چت‌باکس زیبا، با هر زبانی و کاملاً قابل سفارشی‌سازی برای اضافه کردن پشتیبان هوش‌مصنوعی زنده به هر وبسایتی فقط با چند خط کد!

https://github.com/yhyaa79/Supportly.git 


## ویژگی‌های کلیدی
- (RTL)طراحی مدرن با انیمیشن‌های نرم
- پشتیبانی کامل از چت متنی و صوتی (STT + TTS)
- پاسخ‌های هوش مصنوعی با قابلیت استریم (پاسخ لحظه‌ای بدون تأخیر)
- پشتیبانی از مدل‌های مختلف OpenAI, Groq, OpenRouter و …
- محدودیت نرخ (Rate Limit) قابل تنظیم برای جلوگیری از سوءاستفاده
- بدون نیاز به فرانت‌اند فریم‌ورک – فقط یک فایل JS + سرور Flask ساده

## نصب و راه‌اندازی سریع (کمتر از ۵ دقیقه)

### ۱. کلون کردن پروژه
```bash
git clone https://github.com/your-username/persian-ai-chat-support.git
cd persian-ai-chat-support
```

### ۲. نصب وابستگی‌های پایتون
```bash
pip install flask python-dotenv requests openai
```

### ۳. تنظیم کلیدهای API
فایل `.env` در ریشه پروژه بسازید و کلیدها را وارد کنید:
```env
API_KEY_OPENAI=sk-...
API_KEY_OPENROUTER=sk-or-...
API_KEY_AIMLAPI=xxx (اختیاری)
```

### ۴. اجرای سرور محلی
```bash
python app.py
```
سرور روی `http://127.0.0.1:8090` اجرا می‌شود.

### ۵. اضافه کردن ویجت به وبسایت شما
در قسمت `<head>` یا قبل از `</body>` سایتتان این کد را قرار دهید:
```html
<script>
    const nameWeb = "نام سایت شما";   // عنوان چت‌باکس
    const linkWeb = "https://yoursite.com"; // لینک سایت (اختیاری)
    const model = "openai/gpt-4o-mini";     // مدل دلخواه
</script>
<script src="http://127.0.0.1:8090/static/js/chat-widget.js"></script>
```

همین! بعد از بارگذاری صفحه، ویجت چت‌باکس به صورت خودکار با انیمیشن ظاهر می‌شود.

## سفارشی‌سازی

| متغیر             | توضیحات                              | مقدار پیش‌فرض               |
|-------------------|--------------------------------------|-----------------------------|
| `nameWeb`         | عنوان بالای چت‌باکس                  | "پشتیبان"                 |
| `linkWeb`         | لینک سایت (برای sitemap و …)         | جاری صفحه                  |
| `model`           | مدل LLM (از OpenRouter یا OpenAI)   | `openai/gpt-4o-mini`       |
| رنگ اصلی          | در CSS فایل (`#5e35b1`) قابل تغییر   | بنفش گرادیان               |

## دمو آنلاین
به زودی دمو زنده در Vercel/Netlify قرار می‌گیرد.

## محدودیت نرخ (Rate Limit)
- هر کاربر در هر ۱۲ ساعت فقط ۵ پیام رایگان می‌تواند بفرستد.
- قابل تغییر یا غیرفعال کردن کامل در فایل `utils.py`

## تکنولوژی‌های استفاده‌شده
- Frontend: Vanilla JavaScript + CSS3
- Backend: Flask (Python)
- STT/TTS: OpenAI Whisper & TTS
- LLM: OpenRouter (Groq, OpenAI, Anthropic و …)
- Streaming: Server-Sent Events (SSE)

## مشارکت و توسعه
خیلی خوشحال می‌شویم اگر:
- باگ پیدا کردید → Issue باز کنید
- ترجمه به زبان‌های دیگر اضافه کردید
- تم‌های جدید (تیره، آبی، سبز و …) ساختید
- قابلیت‌های جدید مثل آپلود فایل، ایموجی و … اضافه کردید

## لایسنس
MIT License – می‌توانید آزادانه در پروژه‌های شخصی و تجاری استفاده کنید.

## تماس و پشتیبانی
اگر سوالی داشتید یا می‌خواستید نسخه اختصاصی و برندینگ‌شده برای سایت خودتان داشته باشید، در Issues بنویسید یا به من پیام دهید.

موفق باشید! 🚀  
ساخته‌شده با ❤️ در ایران